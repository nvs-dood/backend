version: "3.7"
services:
    mariadb:
        image: mariadb:latest
        restart: always
        volumes:
            - mariadb-data:/var/lib/mysql
        environment:
            - MYSQL_ROOT_PASSWORD=Pass
        ports:
            - 3306:3306
        networks:
            - doodnet
 
    phpmyadmin:
        image: phpmyadmin/phpmyadmin:latest
        ports:
            - 8000:80
        environment:
            - PMA_ARBITRARY=1
            - PMA_HOST=mariadb
            - MYSQL_ROOT_PASSWORD=Pass
        depends_on:
            - mariadb
        networks:
            - doodnet
    dood-backend:
        image: 
        environment:
            - PORT=80
            - DB_CONN_STRING=root:Pass@(mariadb:3306)/
volumes:
  mariadb-data:

networks:
    doodnet: